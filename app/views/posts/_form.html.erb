<%= simple_form_for post do |f| %>
  <%= f.input :title %>
  <%= f.label :content %>
  <%= f.rich_text_area :content, size: "30x10" %>
  <br />
  <%= f.button :submit, class: "btn btn-primary" %>
<% end %>
