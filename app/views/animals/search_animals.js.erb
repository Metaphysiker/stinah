var records_div = ".animals";

$( records_div ).fadeOut( "fast", function() {

    content = $("<%= escape_javascript( render 'animals_in_groups_of_3', animals: @animals ) %>")

    $(records_div).fadeOut("fast", function(){
      $(records_div).empty().append(content).fadeIn("fast");
    });

});
