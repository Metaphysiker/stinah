<div class="row">
  <% animals.each do |animal| %>
    <div class="col-sm-12 col-md-6 col-lg-4 col-xl-3 mb-3">
      <%= render 'show', animal: animal %>
    </div>
  <% end %>
</div>

<%= paginate animals, params: {controller: 'animals', action: 'index'} %>
